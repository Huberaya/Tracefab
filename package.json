{
  "name": "tracefab",
  "private": true,
  "version": "0.1.0",
  "description": "Tracefab supplier data platform foundations",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "api:typecheck": "tsc --noEmit --target ES2020 --module ESNext --moduleResolution bundler --skipLibCheck --esModuleInterop --types node --lib ES2020,DOM api/*.ts api/_lib/*.ts",
    "schema:static": "python3 scripts/validate_schema.py",
    "db:neon:build": "python3 scripts/build_neon_migration.py",
    "db:generate": "prisma generate",
    "db:deploy": "prisma migrate deploy",
    "db:status": "prisma migrate status",
    "db:pull": "prisma db pull",
    "build": "npm run db:generate && npm run typecheck && npm run api:typecheck && npm run schema:static"
  },
  "devDependencies": {
    "prisma": "^6.19.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@clerk/backend": "^3.18.1",
    "@prisma/client": "^6.19.0",
    "@vercel/node": "^13.0.2"
  }
}
